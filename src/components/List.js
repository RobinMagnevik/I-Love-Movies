import React from "react";
import Movie from "./Movie";

function List({ movies }) {
  return (
    <div>
      <div>
        {/* {movies.map((movie) => (
          <Movie key={movie.id} movie={movie} />
        ))} */}
      </div>
    </div>
  );
}

export default List;
